<div class="row">
	<% @posts.each do |post| %>
		<div class="col-sm-6 col-md-3">
			<div class="thumbnail">
				<%= link_to (image_tag post.image.url(:medium), class: 'image'), post %>
			</div>
		</div>
	<% end %>	